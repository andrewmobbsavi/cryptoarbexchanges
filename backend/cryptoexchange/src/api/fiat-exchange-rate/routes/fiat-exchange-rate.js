'use strict';

/**
 * fiat-exchange-rate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::fiat-exchange-rate.fiat-exchange-rate');
